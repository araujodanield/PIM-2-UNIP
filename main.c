#include <stdio.h>
#include <stdlib.h>
#include "hub.h"
#include "servidor.h"
#include "caixa.h"

int selecao; //Variável global utilizada por todos os arquivos do programa, para fins de seleção de opções na interface visual.

int main() {

    hub(); //Informa ao compilador que o primeiro conjunto de código à ser analisado ao iniciar o programa é o prensente em hub.c.
}

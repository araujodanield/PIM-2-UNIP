#ifndef SERVIDOR_H //Para evitar erros de compilação, o comando verifica se SERVIDOR_H não está definido e então informa que deve ser definido uma única vez.
#define SERVIDOR_H //Define SERVIDOR_H.

void servidor(void); //Informa ao programa que existe uma função com o nome servidor() declarada em servidor.c. Neste caso, a aplicação em si.

#endif //Fim da verificação se SERVIDOR_H está definido.

